/*
function loadPokemonDetails(pokemonIndex) {
    // Lógica para carregar os detalhes do Pokémon
    for (let i = 0; i < pokemons.length; i++) {
        const pokemon = pokemon[i];
        pokemon.addEventListener("click", function() {
          // Chamar a função para carregar a página de detalhes do Pokémon correspondente
          loadPokemonDetails(i);
        });
  }
}
  export default loadPokemonDetails;
*/